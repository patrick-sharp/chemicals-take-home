can curl api routes like this:
curl localhost:3000/api/hello

TODO:
figure out if I need to opt out of cache for all routes
CRUD endpoints
  [x] create batch
  [x] update batch
  [x] reset batches
  [x] list batches
  [x] list demand
  [x] list products
display data
  [x] integer input for scheduling batch
  [ ] summary for total demand
  [x] breakdown of each batch
  [x] render each batch nicely
    [x] each status has a different indicator
  [x] ability to move each batch to the next stage


